const { Guild, EmbedBuilder } = require('discord.js');

module.exports = async (client, msg, args, command) => {
  
    if (command === "creater") {
      return msg.channel.send("Developer - Pasquini#3102");
    }
  };